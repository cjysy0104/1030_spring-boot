package com.example.demo;

import org.springframework.context.annotation.Configuration;

@Configuration
public class BeanConfig {

	/*
	 * Spring MVC에서 빈으로 등록할 때: 
	 * 
	 * root-context.xml 가서
	 * 
	 * <bean class="풀클래스명" id="식별자">
	 * 		<property 필드값/>
	 * </bean>
	 * ===================================
	 */
	
	public StudyBean bean() {
		
	}
}
