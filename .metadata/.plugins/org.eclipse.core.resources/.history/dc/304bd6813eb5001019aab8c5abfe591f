package com.example.demo;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("study")
// SpringWebStarter는 기본적으로 JSON형태로 Converting해줄 수 있는 Jackson라이브러리 포함
// produces 기본값이 application/json; charset=UTF-8이기 때문에
// 자동으로 MessageConverter로 변환됨 => 잘 응답됨
public class StudyController {

	/* Spring Setter
	 * 
	 * 특정 기능에 필요한 의존성 라이브러리들을 한번에 관리 할 수 있는 개념
	 * 
	 * 예) spring-boot-tart-web: 웹 애플리케이션 개발에 필요한 의존성만 모아놨음
	 * (Servlet Dispathcer -MVC, Jaxon) 
	 * spring-boot-starter-security: 스프링 시큐리티 관련 의존성들이 모여있음
	 * 
	 * 개발자는 필요한 기능이 있다 => Starter추가 => 의존성 관리를 직접하지 않음
	 * 모든 개발자가 동일한 Starter를 쓰기 때문에 프로젝트 간 의존성 충돌 방지
	 * 
	 * 단, Starter에 모든 라이브러리가 존재하진 않음
	 */
	
	@Autowired
	private StudyBean studyBean;
	
	@GetMapping
	public ResponseEntity<String> getTest(){
		return ResponseEntity.ok("응답 정상");
	}
	
	/*
	 * Spring Boot 장점
	 * 
	 * 
	 */
}
